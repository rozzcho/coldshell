# Solana network the site talks to: localnet (default), devnet or mainnet
VITE_SOLANA_NETWORK=devnet
# RPC endpoint. On devnet use your Helius URL:
# https://devnet.helius-rpc.com/?api-key=YOUR_KEY
VITE_RPC_URL=https://api.devnet.solana.com

# Shortens a day so a whole week can be walked through in an hour. 10 makes a shell 70 minutes.
# Leave unset for real days.
# VITE_DAY_MINUTES=10
